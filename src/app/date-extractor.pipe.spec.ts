import { DateExtractorPipe } from './date-extractor.pipe';

describe('DateExtractorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateExtractorPipe();
    expect(pipe).toBeTruthy();
  });
});
